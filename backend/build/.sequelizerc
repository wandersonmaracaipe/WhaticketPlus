const { resolve } = require("path");

module.exports = {
  // "config": resolve(__dirname, "config", "config.json"), //montado via script generate_config
  "models-path": resolve(__dirname, "whaticketplus", "models"),
  "migrations-path": resolve(__dirname, "whaticketplus", "database", "migrations"),
  "seeders-path": resolve(__dirname, "whaticketplus", "database", "seeds")
};
